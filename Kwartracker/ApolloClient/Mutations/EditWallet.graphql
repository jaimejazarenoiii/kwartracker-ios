mutation EditWallet($editWalletInput: EditWalletInput!){
  editWallet(input:$editWalletInput) {
      currency
    id
    title
    transactions {
      id
      amount
      title
      datetime
      category {
        id
        title
        budgets {
          id
          amount
        }
      }
    }
  }
}
