mutation profileUpdate($input: UpdateProfileInput!) {
    updateProfile(input: $input) {
        id
        age
        email
        firstName
        gender
        lastName
        middleName
    }
}
